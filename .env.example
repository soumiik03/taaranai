# Must exactly match the Webhook secret configured on the GitHub App at github.com (Settings > Developer settings > GitHub Apps > [app name] > General). These are two independently-stored values — regenerating one does not update the other.
GITHUB_WEBHOOK_SECRET=
